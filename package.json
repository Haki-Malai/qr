{
  "name": "qr-project",
  "version": "1.0.0",
  "description": "Project to redirect with visitor fingerprint alert",
  "main": "main.js",
  "scripts": {
    "start": "node build.js && npx serve dist -l 5000",
    "build": "node build.js"
  },
  "dependencies": {
    "@fingerprintjs/fingerprintjs": "^4.6.2",
    "string-hash": "^1.1.3",
    "success-motivational-quotes": "^1.0.8"
  },
  "devDependencies": {
    "esbuild": "^0.25.5"
  }
}
